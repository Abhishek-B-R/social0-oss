# Social0 CLI environment variables
# Copy to .env for local development (optional — prefer `social0 login`)

# API key (sk_live_...) — preferred for CI/headless; not persisted to disk
SOCIAL0_API_KEY=

# Passphrase for encrypted local credential file when OS keychain is unavailable
SOCIAL0_CREDENTIAL_PASSPHRASE=

# API base URL (default: https://api.social0.app/v1)
SOCIAL0_API_URL=

# Request timeout in milliseconds (default: 30000)
SOCIAL0_REQUEST_TIMEOUT_MS=

# Max retries on rate limit (default: 3)
SOCIAL0_MAX_RETRIES=
