# VideoPostForm

| File | Role |
| --- | --- |
| `VideoPostForm.tsx` | Form UI + state/orchestration |
| `types.ts` | Local account / media / caption types |
| `index.ts` | Public exports |

Search for `SECTION:` markers:

- `media upload handlers`
- `submit orchestration`

Shared platform settings UI already lives under `@/components/*Settings*`.
Add new publish networks in `backend/server/src/lib/publish-platforms/`.
