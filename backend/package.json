{
  "name": "social0-backend",
  "private": true,
  "workspaces": [
    "shared",
    "server",
    "engine",
    "worker"
  ],
  "scripts": {
    "build": "bun run --filter @social0/shared build && bun run --filter @social0/worker build && bun run --filter @social0/server build && bun run --filter @social0/engine build",
    "dev:server": "bun run --filter @social0/server dev",
    "dev:engine": "bun run --filter @social0/engine dev",
    "dev": "bun run dev:server & bun run dev:engine",
    "start:server": "bun run --filter @social0/server start",
    "start:engine": "bun run --filter @social0/engine start"
  },
  "engines": {
    "node": ">=20"
  }
}
