{
  "name": "social0-backend",
  "private": true,
  "workspaces": [
    "shared",
    "server"
  ],
  "scripts": {
    "build": "bun run --filter @social0/shared build && bun run --filter @social0/server build",
    "dev:server": "bun run --filter @social0/server dev",
    "dev": "bun run dev:server",
    "start:server": "bun run --filter @social0/server start"
  },
  "engines": {
    "node": ">=20"
  }
}
