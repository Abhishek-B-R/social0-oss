# Local Redis is no longer used - BullMQ + caches use Upstash (see backend/.env.example).
# Kept for optional offline experiments only.
services:
  redis:
    image: redis:7-alpine
    ports:
      - "6379:6379"
    volumes:
      - redis-data:/data

volumes:
  redis-data:
